server:
  port: 8080

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    open-in-view: true
    properties:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.temp.use_jdbc_metadata_defaults: false

surwave:
  authType: basic
